*.py[co]
*~
*.DS_Store
_build/
auto_examples/
gen_api/

# don't track the job output files. sometimes there are lots of them
slurm*

# don't track the virtual environment, as it might become big
venv/

logs/
*.tar.gz
outputs/*/*/
*mdrun_farm
outputs/
